# OmniEmu

OmniEmu is an experimental browser emulator built around one runtime rule:

> Every targeted console is implemented inside the same `omnicore.wasm` emulator binary.

The browser does not download or select a different emulator core per console. Platform selection is data passed into OmniCore. CPU engines, device models, buses, timing, video, audio, media handling, input, execution caches, and state logic are compiled together and share one ABI.

The emu bird is OmniEmu's mascot and project mark.

## Target set

OmniCore now has explicit hardware blueprints for **30 target systems across generations 1–8**, from Magnavox Odyssey/Home Pong through Wii U and Nintendo Switch. PlayStation 4 and Xbox One keep reserved platform ids so old data cannot collide with future ids, but they are intentionally outside the active target set.

A hardware blueprint is not a compatibility claim. It records the guest CPU/accelerator domains, endian/address-width requirements, memory model, graphics class, media model, firmware/key requirements, persistence needs, and high-end execution requirements that the common core must satisfy.

## Current implementation

The unified runtime is already executing real machine graphs and now has substantially broader substrate:

- Rust `cdylib` built as one optimized `omnicore.wasm`.
- 64-bit shared physical interconnect with little- and big-endian 8/16/32/64-bit accesses.
- 16–64-bit virtual-memory/MMU layer with page permissions, mapping generations, and translation faults.
- Exact rational clock domains plus deterministic event scheduling.
- 256-source priority/mask interrupt controller.
- Generic DMA engine with fixed/increment/decrement addressing.
- Versioned, console-tagged deterministic save-state codec.
- JIT-neutral guest IR, per-ISA translated-block cache, MMU-generation validation, and self-modifying-code invalidation primitives.
- GPU command queue with bounded upload memory plus generation-aware shader translation and caching for the shared graphics path.
- Chunked 64-bit resource store for games, BIOS, firmware, keys, discs, NAND, storage, and memory cards, including bounded live paging for large media.
- Sparse random-access/block media primitives whose core data model can represent multi-gigabyte images without allocating every block.
- Sparse persistent block storage suitable for memory cards, flash/NAND images, and virtual hard disks.
- One RGBA video path with WebGPU preferred and WebGL 2 fallback, plus one interleaved `f32` audio path with AudioWorklet preferred and Web Audio fallback.
- One global four-player controller ABI with 64 digital bits and 8 signed 16-bit analog axes per player; keyboard and Gamepad mappings feed the same logical controls.
- Reusable NMOS 6502 execution engine covering the official set plus common stable undocumented LAX/SAX/SLO/RLA/SRE/RRA/DCP/ISC, multi-byte NOP and immediate ALU forms, with NMOS BCD arithmetic that is disabled for the NES 2A03/2A07 path.
- Reusable Z80 execution engine with base/CB/ED/indexed instruction families, interrupt modes, block operations, I/O, save-state serialization, and shared use by Sega/Coleco-class machines.
- Reusable CP1610 execution engine with the Intellivision reset/register model, word-oriented memory, register/direct/indirect/immediate addressing, SDBD handling, status/shift/branch families, interrupt vectoring, instruction timing, and deterministic state serialization.
- Reusable W65C816 execution engine with all 256 opcode decode paths, emulation/native mode transitions, 8/16-bit accumulator and index widths, 24-bit addressing, decimal arithmetic, interrupt/stack flow, long calls/jumps, and block moves as the SNES CPU foundation.
- Reusable Sony SPC700 execution engine with all 256 opcode decode paths, direct-page/bit operations, 8/16-bit arithmetic, branches/calls/stack flow, multiply/divide/decimal adjust, and deterministic state serialization; S-SMP RAM, ports and timers are integrated into the SNES machine with a firmware-free IPL transfer HLE. The S-DSP foundation decodes BRR blocks and mixes eight stereo voices at 32 kHz with hardware-shaped four-point Gaussian interpolation, pitch and PMON modulation, key on/off, counter-scheduled ADSR/GAIN envelopes and noise, ENDX, per-voice echo sends, stereo echo-buffer RAM, eight-tap FIR filtering, echo volume/feedback and deterministic state. Register polling latency, KON/KOFF edge timing and cycle-faithful echo/DAC timing remain.
- Reusable Motorola 68000 execution engine with 24-bit big-endian bus semantics, exception/interrupt flow, effective-address decoding, control flow, arithmetic/logical/shift families, stack operations, and deterministic state support.
- Reusable Hitachi SH-2 execution engine with 32-bit big-endian bus semantics, delayed control flow, arithmetic/logical/multiply/divide families, traps/exceptions, interrupt masking, MAC/GBR/VBR state, and deterministic serialization; it is shared by 32X and the later Saturn path.
- Reusable MIPS R3000A-class execution engine for the fifth-generation path, including branch/load delay behavior, arithmetic/HI-LO, COP0 exceptions/interrupts/RFE, aligned and LWL/LWR/SWL/SWR memory access, and deterministic pipeline state.
- Reusable ARM60/ARMv3 execution engine with conditional execution, barrel shifts, integer ALU/multiply, load/store and block transfers, branches/software interrupts, CPSR/SPSR transfers, IRQ/FIQ exception modes with banked registers, and deterministic state serialization for the 3DO path.
- Reusable Jaguar Tom/Jerry RISC execution engine shared by GPU and DSP, with the 64-opcode integer/memory/control map, alternate register banks, Jaguar condition codes and delay slots, phrase transfers, matrix/divide/modulo state, GPU/DSP-specific opcodes, hardware interrupt masks/latches, bank-zero interrupt entry and internal-RAM vectors, and deterministic serialization.
- Playable Magnavox Odyssey discrete-logic machine with six base game-card wiring configurations, player spots, ball/wall/gate/English behavior, silent output, controls, reset, and deterministic state.
- Playable built-in Home Pong/Telstar-class hardware simulation with video, audio, input, reset, and save states.
- Runnable Atari 2600 development machine with a 6507-style 13-bit bus over the shared 6502, RIOT RAM/I/O/timer, color-clocked TIA playfield/players/missiles/ball/collisions, WSYNC stalls, two-channel TIA audio, joystick/fire, four RC-timed paddle inputs, and XG-1 light-gun trigger/beam sensing through pointer input, deterministic state, and 2K/4K, F8/F6/F4, SuperChip, Parker E0, M-Network E7, 3E with 32 KiB banked RAM, four-segment 3E+, Tigervision 3F, EF/EFSC, DF/DFSC, BF/BFSC, SB, classic DPC fetcher/music hardware, Activision FE, UA, Dynacom F0, and CBS FA cartridge banking.
- Runnable Atari 5200 development machine reusing the NMOS 6502 with 16 KiB RAM and a 2 KiB BIOS, ANTIC scanline/display-list foundation, GTIA color/trigger registers, POKEY analog POT input plus four-controller-selected 15-key keypad/control KBCODE and top-trigger Break IRQ behavior, four-channel audio/IRQ, WSYNC, Atari `CART` type 4/6/7/16/19/20/71-74/159 parsing, standard 4/8/16/32 KiB cartridge mappings, CRC-profiled raw 16 KiB commercial layouts, both Bounty Bob 40 KiB wirings, and raw or `CART` 64/128/256/512 KiB SuperCart banking with read/write `$BFC0-$BFFF` switching and deterministic state.
- Runnable Atari 7800 development machine reusing the NMOS 6502/SALLY path with the console RAM mirrors, PIA input/timer, TIA audio/input with legacy one-button and independent two-button ProLine modes, MARIA DLL/display-list rendering and DLI/WSYNC foundation, A78 v3/v4 parsing, Linear/SuperGame/Activision/Absolute cartridges, CPU/MARIA Bankset ROM views, ordinary 16 KiB RAM, 8 KiB A8-mirrored EXRAM, two-bank 32 KiB EXRAM/X2 selected by SuperGame bit 5, CPU/MARIA halt-banked EXRAM, and the 512 KiB Souper mapper with 32 KiB paged EXRAM plus MARIA MFT/character-bank remapping. A78 region byte 57 selects NTSC or PAL CPU/POKEY/TIA clocks and 262/312-line MARIA frame timing; multiple POKEY locations, valid Bankset EXFIX/BANK6 profiles, and deterministic mapper/RAM/controller state are supported. Spec-invalid Bankset EXROM/X2 combinations fail closed; CoreTone/BupChip audio and PAL-specific palette/overscan fidelity remain outside the cartridge path.
- Runnable NES development machine with CPU/PPU bus, controllers, cycle-timed OAM/DMC DMA, NMI/IRQ, dot-timed background/sprite rendering, `v`/`t`/fine-X background fetch scrolling, mapper-visible PPU pattern fetches, four mirroring modes, pulse/triangle/noise/DMC audio, deterministic save states, battery-backed persistence, and mapper 0/1/2/3/4/5/7/9/10/11/13/16/18/19/21/22/23/24/25/26/32/33/34/64/65/66/67/68/69/70/71/73/75/76/78/79/80/82/85/87/88/89/93/94/95/113/118/119/140/146/152/154/155/158/180/184/185/206/207/210/232/241/552 support.
- Runnable Master System development machine with the shared Z80, Sega ROM/SRAM mapper, Mode 4 VDP background/sprite rendering, 3:2 CPU-to-dot timing, frame/line interrupts, scroll locks and counters, active-low two-player input plus dual-port Light Phaser trigger/optical TH sensing with H-counter latching, SN76489 PSG audio, save states, and persistent cartridge RAM.
- Runnable ColecoVision development machine reusing the Z80 and SN76489 plus a reusable TMS9918 video device with Graphics I/II tile rendering, sprites, VDP NMI, standard cartridge mapping plus 64 KiB–1 MiB MegaCart banking, joystick/two-button/12-key controls with hardware strobe multiplexing, Super Action extra fire buttons, spinner/roller direction and pulse timing, audio and save states.
- Runnable Intellivision development machine with CP1610 execution, user-supplied EXEC/GROM images, flat cartridge loading plus Intellicart `.ROM` segment CRC validation, read/write/narrow attributes and bank switching, scratch/system RAM, GRAM aliases, STIC background cards/MOBs/collisions, AY-3-8914 tone/noise/envelope audio, active-low disc/fire/12-key controller matrices, and deterministic save states including writable cartridge memory. STIC bus arbitration/timing, expansion peripherals, and broad cartridge validation remain foundation work.
- Runnable Genesis / Mega Drive development machine combining the shared 68000 and Z80 with ROM/SMD loading, work RAM, controller I/O, Z80 bus request/reset, VDP VRAM/CRAM/VSRAM planes/sprites/interrupt foundations, header-selected NTSC/PAL region timing with the hardware version bit, 262/313-line VDP frames and region-specific 68000/Z80/YM2612/SN76489 clocks, YM2612 FM/DAC/timers, SN76489 audio, SRAM persistence, the official Super Street Fighter II 512 KiB bank mapper, and deterministic save states.
- Runnable Sega CD / Mega-CD development machine extending the Genesis graph with a second 68000, user BIOS, 512 KiB PRG RAM, 256 KiB shared Word RAM with 1M/2M ownership modes, 8 KiB backup RAM, gate-array communication/timer/IRQ state, demand-paged ISO/raw and multitrack CUE BINARY/WAVE media with CD-DA playback, RF5C164-compatible eight-channel PCM, and deterministic state/persistence.
- Runnable Sega 32X development machine extending the same Genesis graph with two shared SH-2 engines, MARS startup/communication/interrupt registers, 256 KiB SDRAM, dual 128 KiB framebuffers, palette/packed/direct/RLE display paths, ROM banking, documented overwrite semantics, PWM FIFO audio, and deterministic state.
- Runnable PC Engine / TurboGrafx-16 HuCard and CD-ROM development machine with a dedicated HuC6280 execution layer over the shared 6502 substrate, eight MPRs, relocated direct/stack pages, timer/IRQ and major HuC6280/65C02 extensions, HuC6270 VRAM/BAT/sprite/DMA/raster foundations, HuC6260 palette, six-channel PSG, two-button joypad mux plus Street Fighter II six-button Avenue Pad scanning, standard HuCards plus the 2.5 MiB Street Fighter II bank window and canonical Populous 32 KiB cartridge RAM, System Card 3 memory/register mapping, Arcade Card 2 MiB DRAM with four programmable access ports plus shift/rotate registers, SCSI READ(6) transport, demand-paged ISO/raw-2352 and multitrack CUE BINARY/WAVE media, track-aware directory/SubQ reporting, stereo CD-DA playback mixed into the public 48 kHz output, 2 KiB backup RAM persistence, MSM5205-compatible 4-bit ADPCM decode/playback with a programmable rate divider, CPU-port read/write pipeline delays, CD-to-ADPCM DMA, full-play IRQ, CD-DA/ADPCM fader commands, and deterministic state.
- Runnable PC Engine SuperGrafx development machine reusing the HuC6280, HuC6260 and PSG foundation with two independent HuC6270 VDCs, HuC6202 VPC register/window priority control, ST-port-selected VDC access, combined video IRQ routing, deterministic dual-VDC composition, and save states.
- Runnable Neo Geo NEO1 cartridge development machine combining the shared 68000 and Z80 with BIOS/cartridge vector switching, work/backup/card memory, bank window, VRAM and dual palette banks, FIX/sprite rendering, vblank/timer IRQs, sound-command NMI/reply, and YM2610 SSG plus ADPCM-A/B sample playback from V1/V2 ROM sections.
- Runnable SNES / Super Famicom development machine using the shared W65C816 with LoROM/HiROM/ExHiROM cartridge mapping, 128 KiB WRAM, CPU/PPU I/O, controllers, vblank/NMI timing, SRAM persistence, and deterministic save states. The PPU covers BG1-BG4 modes 0-6, 32/64-tile maps, 8/16-pixel characters, 2/4/8bpp planar decoding, offset-per-tile modes 2/4/6, native modes 5/6 high resolution and pseudo-hires, mosaic, two-window logic, main/sub-screen composition, color math, direct color, Mode 7/EXTBG, and OBJ rendering with priority rotation plus 32-sprite/34-sliver scanline limits. General DMA and direct/indirect HDMA are implemented, with SA-1 and OBC1 enhancement-chip paths plus functional DSP-1, DSP-2 and DSP-3 command engines and S-RTC cartridge clock support. DSP-1 covers arithmetic/trigonometry, matrix transforms, projection/raster commands and the small/large-LoROM and HiROM bus layouts; DSP-2 covers bitmap conversion, transparent overlay, reverse, multiply and fixed-point scaling through its documented LoROM data-port mirrors; DSP-3 covers coordinate/movement operations, its verified 1,024-word data ROM, planar conversion, bitstream decode and path-search commands through the documented LoROM data/status windows. All three preserve deterministic command state. S-RTC exposes its `$2800/$2801` serial protocol, cycle-driven calendar progression, deterministic save state, and dedicated persistent-storage slot. The SPC700 executes through integrated S-SMP RAM/ports/timers after firmware-free IPL upload HLE, and the S-DSP provides BRR playback, Gaussian interpolation, PMON, ADSR/GAIN/noise, echo RAM and eight-tap FIR processing. Remaining foundation work is concentrated in exact PPU/DMA/APU sub-cycle timing and register-latch errata, bit-exact DSP-1 projection/inverse edge behavior, more enhancement chips and peripherals, and representative compatibility-corpus validation.
- Runnable PlayStation development machine using the shared MIPS R3000A with 2 MiB mirrored RAM, scratchpad and user-supplied 512 KiB BIOS mapping, COP0 interrupts, BIOS memory-control/I-cache/cache-isolation behavior, native partial-MMIO store semantics, COP2/GTE register transport with delayed writes, CPU/GTE overlap, hardware-measured input-latch timing for perspective/clipping/depth and non-aliasing lighting inputs, RTPS/RTPT projection, NCLIP/AVSZ, MVMVA/vector math, lighting/color/depth-cue commands, UNR division and FLAG saturation behavior, GPU VRAM/display with fill, flat/Gouraud polygon, fixed-point line/polyline DDA, 12-bit-quantized polygon color/UV gradients, 480i active-field drawing rules/GPUSTAT field parity, measured GP0 command busy/readiness timing, rectangle/sprite primitives, 4/8/15-bit texture pages, CLUTs, texture windows, raw/modulated textures, semi-transparency, drawing area/offset, mask-bit rules, VRAM transfers and drawing-environment readback/status, plus DPCR-priority/gated MDEC/GPU/CD-ROM/SPU/OTC DMA with DICR completion IRQs, BFRD gating, RAM_SIZE windows and bus errors. Scanout covers the documented horizontal resolutions, programmable display ranges, PAL/NTSC frame pacing, interlace state and packed 24-bit output. Three root counters expose current/mode/target MMIO, system/dot/HBlank/system-divide-by-eight clock sources, HBlank/VBlank synchronization modes, target/overflow flags, timer IRQ routing and deterministic state. MDEC implements command/status MMIO, quantization/scale tables, RLE/zigzag/IDCT decode, monochrome and color macroblocks, 4/8/24/15-bit output, DMA0 input, DMA1 output, and deterministic state. Two-port SIO0 with digital-pad compatibility plus DualShock-class analog mode/config commands, four stick axes, L3/R3 and two-motor rumble mapping state; a formatted 128 KiB Sony-style memory card with the in-game 81h read/write/ID protocol, checksums, card-status flag and host persistence; streamed CD-ROM sector/controller/DMA/IRQ with queued command/interrupt handshakes, staged INT3-to-INT2/INT5 completion for MotorOn/Stop/Pause/Init/SetSession/Seek/GetID/GetQ/ReadTOC, raw-sector CD-DA Play/Mute/Demute with the controller ATV volume matrix, and XA-ADPCM Mode 2 audio with file/channel filtering, 4/8-bit decode, 18.9/37.8 kHz handling, zig-zag resampling, ADPMUTE and SPU CD-input routing are implemented. Multitrack CUE loading supports BINARY data/audio tracks, 44.1 kHz stereo PCM WAVE tracks, INDEX 00/01, PREGAP/POSTGAP, TOC/subchannel position reporting, track-select playback and AutoPause through the same demand-paged disc resource. Same-basename SBI companions are parsed for LibCrypt bad-Q sectors, and the controller retains the last valid SubQ position across those intentionally bad-CRC sectors. The 24-voice SPU foundation includes 512 KiB sound RAM, SPU-ADPCM decoding, key/envelope/pitch/noise state, Gaussian interpolation, fixed/swept stereo volume state, reverb RAM/register processing, sound-RAM DMA, IRQ9, stereo mixing, and deterministic save states. Aliasing/multi-read GTE latch edges, full asynchronous GPU FIFO/command arbitration, sub-scanline MDEC/timer timing, exact reverb edge timing, CHD/PBP and full raw-subchannel media support, and broad compatibility validation remain foundation work.
- Runnable Nintendo 64 development machine with a reusable 64-bit R4300-class CPU, big-endian memory, COP0/TLB/LL-SC and Count/Compare interrupt state, COP1 floating-point register/control state with 32/64-bit moves, load/store, S/D arithmetic, compare/branch and integer conversion paths, reusable scalar/vector RSP execution, firmware-free PIF boot HLE into the cartridge IPL3 with common CIC seed profiles and the 6105 SP-IMEM helper, 8 MiB RDRAM, SP/DP/MI/VI/AI/PI/SI register and DMA foundations, Joybus controller/Controller Pak paths, 4 Kbit/16 Kbit cartridge EEPROM with write-busy timing, SRAM and 128 KiB FlashRAM persistence, Animal Forest cartridge RTC with Joybus block access and cycle-driven BCD calendar, RDP color-image/fill/texture-rectangle commands plus fill/shade/texture/Z triangle packet families, combiner/alpha/blend foundations, VI presentation, AI audio, and deterministic save states. Bit-exact VR4300 floating-point exception/subnormal behavior, exact RSP reciprocal/vector edge behavior, residual RDP coverage and timing fidelity, uncommon CIC/PIF variants, Transfer Pak and other peripheral breadth, exact timing/arbitration, and broad compatibility validation remain foundation work.
- Runnable Sega Saturn development machine using two shared SH-2 engines with a user-supplied 512 KiB BIOS, low/high work RAM, SMPC processor/input control, SCU interrupt and three-channel DMA foundations, VDP1 command/framebuffer rendering composed by VDP2, 32-slot SCSP PCM with LFO/FM modulation, sound DMA, programmable effects-DSP execution and a sound-68000 bus, backup-RAM persistence, streamed ISO/raw-sector and multitrack CUE BINARY/WAVE CD access with 44.1 kHz CD-DA routed through the SCSP external-input path, and deterministic save states. Complete VDP1/VDP2 modes, remaining SCSP DAC/fine-timing fidelity, SH-2 on-chip peripheral edge behavior, exact SCU/SMPC timing, complete CD Block commands, CHD media, and broad compatibility validation remain foundation work.
- Runnable Atari Jaguar cartridge development machine using the shared Motorola 68000 plus reusable Tom GPU/Jerry DSP RISC engines, 2 MiB DRAM with ROMHI boot overlay, 6 MiB cartridge window, a user-supplied 128 KiB BIOS, Tom register/CLUT/line-buffer state and bitmap/scaled-bitmap object rendering, active-low controller matrix, Jerry stereo DAC output, both programmable Jerry interval timers, JINTCTRL host interrupt routing, DSP timer/I2S interrupt vectoring, internal serial-clock cadence, cartridge EEPROM persistence, and deterministic save states. Complete object-processor update/branch semantics, blitter behavior, asynchronous-serial breadth, exact bus arbitration/timing, Jaguar CD, and broad compatibility validation remain foundation work.
- Runnable 3DO development machine using the shared ARM60/ARMv3 engine with 2 MiB DRAM, a dual-ported VRAM window, 1 MiB BIOS boot overlay/high bank, 32 KiB NVRAM persistence, MADAM CEL/register foundations, CLIO controller/FIQ state, 16-voice DSPP PCM, SPORT aliasing, and streamed ISO/raw or multitrack CUE XBus data-sector access through the shared CD-image layer, plus 640x480 output and deterministic save states. Complete CEL/VDLP behavior, programmable DSPP execution, exact MADAM/CLIO timing, full XBus/CD commands, CD-DA playback, CHD media, and broad compatibility validation remain foundation work.
- Runnable Dreamcast development machine with dedicated SH-4 execution, ARM7TDMI/Thumb AICA execution, HOLLY/Maple/AICA/GD-ROM foundations, demand-paged disc access, framebuffer/audio output, flash persistence, and deterministic state.
- Runnable PlayStation 2 HLE development machine with a 128-bit R5900/Emotion Engine core, EE RAM/scratchpad/INTC/timer foundations, raw ELF or ISO9660 `SYSTEM.CNF`/`BOOT2` loading, GIF A+D/image transfers, GIF DMAC channel-2 normal/source-chain transfers with DMAtag CALL/RET/TIE handling and D_STAT interrupts, a PSMCT32 GS framebuffer path, memory-card persistence, and deterministic state. BIOS boot, IOP/SIF/SIO2, vector-unit execution, complete GS behavior, SPU2, CDVD breadth, exact timing, and broad validation remain.
- Runnable GameCube development machine with PowerPC 750/Gekko execution, paired-single and quantized GQR paths, hardware-table reciprocal and reciprocal-square-root estimates, full-width single-precision FPR operands, 25-bit frC multiply/FMA handling, fused scalar/paired FMA rounding, FPSCR rounding/NI behavior, disabled single-precision overflow/underflow/inexact status with FI/FR/XX, FPRF/FPCC classification, NaN-aware ordered/unordered compares, invalid arithmetic status, and corrected opcode-63 A-form decoding; MEM1/ARAM, Flipper PI/DI/SI/VI/DSP foundations, a GX write-gather path with CP/BP/XF/indexed loads, VCD/VAT vertex framing, display-list traversal, direct/indexed position-color primitive rasterization into the EFB, XF transform/projection/viewport handling, and BP-driven YUYV EFB-to-XFB copies, raw DOL/disc bootstrap with HLE BS2 Disc-ID/DTK reconstruction, streamed DVD DMA with drive-state/error/seek/DTK command handling, demand-paged 48 kHz stereo DTK ADPCM decoding/mixing and block-timed stream position, controller input, XFB scanout, audio DMA, channel-0/channel-1 EXI handling with independent Slot A/Slot B memory-card identification/status/read/program/erase paths and persistence, channel-2 AD16 ID/read/write trace-register transfers, optional user-supplied 2 MiB IPL ROM access with raw boot-code descrambling and embedded font reads, IPL UART/eUART debug-register handling, channel-0 chip-select-2 IPL SRAM access with a deterministic RTC, and deterministic state. Remaining work includes Gekko enabled overflow/underflow/inexact exception-result delivery, paired-single rounding-flag aggregation, pathological subnormal/FMA exponent cases, full GX/Flipper graphics, DSP microcode, additional EXI devices, exact timing, compressed disc containers, and broad validation.
- Runnable Xbox development machine with reusable 32-bit x86 execution, XBE section loading and entry decoding, 64 MiB shared RAM, NV2A/MCPX register apertures, PCRTC framebuffer scanout, and deterministic state.
- Runnable Wii homebrew development machine with Broadway-compatible PowerPC execution, MEM1/MEM2, Flipper/Hollywood register windows, PPC-to-Starlet IPC boundary, XFB scanout, audio surface, and deterministic state.
- Runnable Xbox 360 homebrew development machine with three Xenon PowerPC64 core objects, 512 MiB sparse unified guest memory, ELF64/PowerPC loading, public video/audio surfaces, and deterministic state.
- Runnable PlayStation 3 homebrew development machine with Cell PPE PowerPC64 execution, sparse 256 MiB XDR and 256 MiB RSX memory, seven visible SPU local stores, ELF64 loading, and deterministic state.
- Runnable Wii U homebrew development machine with three Espresso PowerPC core objects, sparse 32 MiB MEM1 and 2 GiB MEM2, ELF32/PowerPC loading, public video/audio surfaces, and deterministic state.
- Runnable Nintendo Switch homebrew development machine with reusable AArch64 execution, four application-core state objects, sparse 4 GiB DRAM, NRO segment/BSS loading, homebrew ABI entry state, deterministic state, and Horizon HLE foundations for service management, applet/window control, time, filesystem, HID shared-memory input, and VI/Binder display-layer initialization.

The browser stages cartridge-sized local files into OmniCore in bounded chunks instead of first duplicating them into one temporary WASM allocation. Disc-class resources use bounded streaming storage: when a machine reaches an unstaged range, OmniCore reports the missing byte interval and the browser hydrates a local `File` window before execution resumes. The same resource ABI supports multiple firmware, key, disc, and storage slots. Machine-owned persistent resources have a generic ABI; every exposed storage, memory-card, and NAND resource in the current slot range is restored and checkpointed independently in IndexedDB using a content-derived game fingerprint. This includes multi-resource machines such as GameCube Slot A/Slot B/IPL SRAM, Nintendo 64 save/RTC/Controller Pak storage, and Neo Geo backup RAM plus memory card.

## What "one core" means

Consoles do not contain identical hardware, so a universal emulator cannot be one generic CPU/GPU algorithm. OmniCore provides a common kernel and reusable hardware primitives, then constructs the selected console's real hardware graph inside the same executable.

Source files remain separated for maintainability, but they are not runtime plugins or independently loaded emulator modules. They are statically linked into one program. A console implementation is another machine graph compiled into OmniCore and reached through the same platform/resource ABI.

The execution architecture is deliberately heterogeneous. A PS2 blueprint can contain R5900, R3000-class IOP, and vector-unit domains; PS3 can contain PowerPC and SPU domains; Switch can use a 64-bit ARM domain. They still share the same scheduler, MMU/interconnect abstractions, resource system, input/state contract, and WebAssembly runtime.

## Support and launchability

Compatibility state and developer launchability are intentionally separate:

- `playable` — a machine is exposed as a normal supported system.
- `foundation` — a runnable OmniCore machine graph exists, but timing, peripheral, or compatibility validation is incomplete.
- `planned` — the platform has a target blueprint, but the machine graph is not complete.
NES is `foundation`: NROM, MMC1/MMC1A including SUROM/SOROM/SXROM/SZROM and the defined hard-wired submappers, UxROM, CNROM including mapper 185 copy-protection gating and bus-conflict variants, MMC2, MMC3/MMC6, MMC4, MMC5/ExROM with four PRG/CHR modes, banked PRG-RAM protection, ExRAM/fill nametables, extended attributes, vertical split, scanline IRQ, multiplier and pulse/PCM expansion audio, Tengen RAMBO-1 mappers 64/158, Nintendo MMC3 multicart mappers 37/47, Nintendo TxSROM mapper 118, TQROM mapper 119 with mixed CHR ROM/RAM bank selection, Taito X1-005 mappers 80/207 with internal RAM and alternate CIRAM routing, Taito X1-017 mappers 82/552 with protected 5 KiB RAM and IRQ support, Konami VRC1/VRC2/VRC3/VRC4/VRC6/VRC7, Jaleco SS88006, JF-13 mapper 86 and JF-17/JF-19 mappers 72/92, Irem H3001, G-101 and TAM-S1 mapper 97, Taito TC0190/TC0690 mappers 33/48, Bandai FCG-1/2 and LZ93D50 mapper 16 with 24C02 EEPROM persistence, Bandai 74161/32 mappers 70/152 with bus conflicts and one-screen variants, mapper 78 Holy Diver/Uchuusen board variants with NES 2.0 and legacy mirroring identification, Oeka Kids mapper 96 with address-bus-driven CHR-RAM banking, Namco 163 mapper 19 with arbitrary CHR/nametable routing, cycle IRQ, protected WRAM, battery-backed chip RAM and multiplexed wavetable audio, Sunsoft-2 mappers 89/93 with CHR-ROM banking or electrically gated CHR RAM, Sunsoft-3 mapper 67 with wrap-triggered cycle IRQs, Sunsoft-4 mapper 68 with CHR-ROM-backed nametables, Sunsoft FME-7/5B, Namco 108-family mappers 76/88/154/206 plus NAMCOT-3425 mapper 95 and Namco 175/340 mapper 210, AVE NINA-03/06 and Sachen 146, HES NTD-8, Camerica Quattro, AxROM, CPROM, BNROM/NINA-001, Color Dreams, GxROM, Camerica-class and several discrete mapper boards execute inside OmniCore. Dot-timed visible rendering, the background fetch/scroll pipeline, mapper-visible pattern activity, PPU I/O open-bus behavior, the `$2002` vblank/NMI suppression window, PPUMASK grayscale and NTSC color-emphasis output, rendering-disabled palette-address backdrop override, rendering-time OAMDATA/OAMDMA write suppression, MMC3/TxSROM A12 IRQ variants, RAMBO-1 scanline/cycle IRQs, Bandai FCG/LZ93D50 CPU-cycle IRQs, Taito TC0690 delayed scanline IRQs, Jaleco SS88006/VRC3/VRC4/VRC6/VRC7/Sunsoft-3/FME-7 IRQs, VRC6 pulse/saw, VRC7 six-channel FM, Namco 163 multiplexed wavetable, and Sunsoft 5B tone/noise/envelope expansion audio, cycle-timed OAM/DMC DMA, APU channels including DMC, CPU-cycle noise periods, triangle DAC hold/ultrasonic timer behavior, the 90 Hz/440 Hz high-pass and 14 kHz low-pass output chain, and header-selected NTSC/PAL/Dendy CPU, PPU, frame-counter, noise and DMC timing, deterministic save states, NES 2.0 sizing, and battery persistence are implemented. Remaining compatibility work includes sub-cycle PPU/OAM register races and internal sprite-evaluation bus behavior, analog composite-palette/emphasis fidelity, residual APU timing and analog-output detail, region-specific electrical/video nuances, unstable electrical undocumented-opcode cases, Oeka Kids tablet input, Jaleco uPD7756 speech playback, expansion-audio/advanced mappers, and a representative legal compatibility corpus before promotion to `playable`.

Atari 2600 is `foundation` and launchable: 2K/4K, F8/F6/F4, 128-byte SuperChip variants, Parker E0, M-Network E7 with banked cartridge RAM, 3E with 32 KiB of selectable 1 KiB RAM banks, four-segment 3E+ with 64 KiB ROM and 32 KiB RAM addressing, Tigervision 3F, EF/EFSC, DF/DFSC, BF/BFSC, SB, classic DPC with eight display fetchers, LFSR random data and three 20 kHz music-mode fetchers, Activision FE, UA, Dynacom F0 and CBS FA cartridges run through the shared 6502 with RIOT and a color-clocked TIA path for playfield, player/missile/ball graphics, collision latches, WSYNC, joystick/fire inputs, four RC-timed paddle potentiometers with VBLANK capacitor dump behavior, XG-1 light-gun trigger wiring and active-low beam sensing, and audio. Cartridge banking/RAM, paddle capacitor state, and light-gun target state are deterministic across save states. It still needs modern coprocessor hardware such as DPC+ and related ARM-assisted schemes, the remaining unstable undocumented 6502 forms, exact TIA write delays/HMOVE behavior, PAL/SECAM timing, and a broad compatibility corpus.

Master System is also `foundation`: the shared Z80, Sega mapper/SRAM, Mode 4 tile and sprite rendering, legacy Graphics I/II, text and multicolor modes with legacy sprites, corrected 3:2 CPU/VDP-dot timing, 192/224/240-line timing paths, frame/line interrupts, scroll locks, H-counter TH latching, dual-port Light Phaser TL trigger plus active-low TH optical sensing, Pause/Reset inputs, Korean/Janggun/MSX/Nemesis mapper profiles, SN76489 audio, optional YM2413 FM with hardware output control, save states, and persistence are live. It still needs cycle-level VDP validation, explicit PAL/region configuration, remaining peripheral and cartridge edge cases, and a representative compatibility corpus.

ColecoVision is `foundation` and launchable with a user-provided 8 KiB BIOS: its shared Z80/SN76489 path, TMS9918 Graphics I/II rendering, sprites, VDP NMI, standard cartridge mapping, 64 KiB–1 MiB MegaCart fixed/switchable 16 KiB banking, joystick/fire and full 12-key keypad matrix with hardware strobe behavior, Super Action extra fire buttons, spinner/roller direction with D7/IRQ pulse timing, and Driving Controller steering plus digital accelerator behavior are live with deterministic state. It still needs tighter TMS9918 timing/status accuracy, remaining specialty cartridge hardware and compatibility-corpus validation.

Intellivision is `foundation` and launchable with user-provided EXEC and GROM images: CP1610 execution, flat cartridge loading, Intellicart `.ROM` segment and enable-table CRC validation, readable/writable/narrow memory attributes, Intellicart bank-switch registers, STIC background/MOB rendering and collision state, GRAM aliases, AY-3-8914 tone/noise/envelope audio, active-low disc/fire/full 12-key controller matrices, and deterministic state are implemented. It still needs cycle-faithful STIC bus arbitration and vblank phases, optional expansion peripherals, additional non-Intellicart cartridge/metadata profiles, and representative compatibility-corpus validation.

Genesis is `foundation` and launchable: shared 68000/Z80 execution, controller and Z80 arbitration paths, VDP planes/sprites/interrupts, header-selected NTSC/PAL timing, PAL hardware-version reporting, 224-line NTSC/PAL V-counter discontinuities, region-specific 68000/Z80/YM2612/SN76489 clocks, cartridge SRAM, SMD decoding, the official Super Street Fighter II 512 KiB bank mapper and deterministic state are live. Multi-region cartridges default to NTSC unless PAL is the only declared region. It still needs cycle-exact VDP/DMA behavior, full YM2612 fidelity, PAL encoder/palette and overscan fidelity, uncommon cartridge hardware and broad compatibility validation.

Sega CD is `foundation` and launchable with a user-provided BIOS and ISO/raw or multitrack CUE disc image: the second 68000, shared PRG/Word RAM, gate-array communication/interrupt state, RF5C164-compatible PCM, backup RAM, demand-paged sector reads and CD-DA playback are live. It still needs complete CDC/CDD behavior, CHD media, stamp-map graphics, exact bus arbitration and a representative compatibility corpus.

Sega 32X is `foundation` and launchable: two shared SH-2 engines, MARS register/interrupt communication, SDRAM, ROM banking, double-buffered packed/direct/RLE framebuffer composition, PWM FIFO audio and deterministic state execute in the same OmniCore build. It still needs SH-2 peripheral/cache timing, DMA/serial details, exact video/PWM timing, cartridge edge cases and broad compatibility validation.

PC Engine is `foundation` and launchable for HuCard and CD-ROM development: HuC6280 MPR/timer/IRQ/speed behavior and major extension opcodes, HuC6270/HuC6260 video, PSG audio, two-button input plus the Street Fighter II six-button scan protocol, standard HuCards plus Street Fighter II bank switching and canonical Populous cartridge RAM, DMA, System Card 3 memory/register mapping, Arcade Card 2 MiB DRAM with four programmable access ports plus shift/rotate registers, SCSI READ(6), demand-paged ISO/raw-2352 and multitrack CUE media, CD-DA playback, backup RAM persistence, MSM5205-compatible ADPCM decode/playback with programmable rate, CPU-port pipeline delays, CD-to-ADPCM DMA, full-play IRQ, fader commands and deterministic state are live. It still needs hardware-validated ADPCM/fader timing, exact CD/SCSI and VDC/VCE timing, CHD media, broader System Card variants, remaining specialty HuCard/accessory profiles and representative compatibility-corpus validation.

SuperGrafx is `foundation` and launchable from HuCard images: the PC Engine HuC6280, HuC6260 palette and PSG foundation is extended with two independent HuC6270 VDCs plus HuC6202 VPC register/window priority control, ST-port-selected VDC access, combined video IRQ routing, deterministic dual-VDC composition and save states. It still needs exact VDC/VPC dot timing, SuperGrafx CD expansion behavior, remaining specialty cartridge/accessory profiles and representative compatibility-corpus validation.

Neo Geo is `foundation` and launchable from a user-supplied 128 KiB BIOS plus a NEO1 cartridge image: 68000/Z80 execution, BIOS-vector latching, FIX/sprite/palette rendering, YM2610 SSG and ADPCM-A/B sample playback, backup RAM, memory-card persistence and deterministic state are live. It still needs YM2610 FM fidelity, protected/SMA/PVC/CMC cartridge hardware, raw ZIP-set assembly, exact raster/video timing and representative compatibility-corpus validation.

Nintendo 64 is `foundation` and launchable from `.z64`, `.v64`, and `.n64` cartridge images without bundled PIF firmware: 64-bit R4300 execution, COP0/TLB/LL-SC state, COP1 register/control state with 32/64-bit moves and loads/stores, S/D arithmetic, comparisons/branches and integer conversions, firmware-free PIF boot HLE into IPL3 with common CIC seed profiles and 6105 support, scalar/vector RSP execution, SP/DP/MI/VI/AI/PI/SI register and DMA paths, Joybus controllers and Controller Pak, 4 Kbit/16 Kbit EEPROM with write-busy behavior, SRAM and FlashRAM cartridge persistence, Animal Forest cartridge RTC with Joybus status/read/write, control locks, BCD calendar ticking and persistent RTC RAM, RDP fill/color-image/texture-rectangle commands and fill/shade/texture/Z triangle families with depth, combiner, alpha-threshold and forced-blend paths, VI framebuffer presentation, and deterministic state are live. It still needs bit-exact VR4300 floating-point flags/traps/subnormal and NaN quirks, exact RSP reciprocal/vector edge behavior, residual RDP coverage/timing fidelity, uncommon CIC/PIF variants, Transfer Pak and other peripheral breadth, exact bus/timing arbitration, and representative compatibility-corpus validation.

Saturn is `foundation` and launchable with a user-supplied 512 KiB BIOS: dual SH-2 execution, SMPC processor/input control, SCU interrupt/DMA state, VDP1 command rendering composed through VDP2, SCSP PCM with LFO/FM modulation, sound DMA, programmable effects-DSP execution and a sound-68000 bus, backup RAM, streamed ISO/raw-sector and multitrack CUE BINARY/WAVE media, 44.1 kHz CD-DA through the SCSP external-input path, and deterministic state are live. It still needs full VDP1/VDP2 mode coverage, remaining SCSP DAC/fine-timing fidelity, SH-2 on-chip peripheral edge behavior, exact SCU/SMPC timing, complete CD Block behavior, CHD support and representative compatibility-corpus validation.

Jaguar is `foundation` and launchable with a user-supplied 128 KiB BIOS: the shared 68000, Tom GPU and Jerry DSP RISC execution, ROMHI boot mapping, DRAM/cartridge memory, Tom object-list video foundation, active-low controller matrix, Jerry DAC audio, programmable Jerry PITs, JINTCTRL host routing, DSP timer/I2S interrupt entry, serial-clock timing, EEPROM persistence and deterministic state are live. It still needs full object-processor update/branch behavior, blitter fidelity, asynchronous-serial breadth, exact arbitration/timing, Jaguar CD and representative compatibility-corpus validation.

3DO is `foundation` and launchable with a user-supplied 1 MiB BIOS: ARM60/ARMv3 execution, boot-ROM overlay, DRAM/VRAM/NVRAM, MADAM CEL/register foundations, CLIO controller/FIQ state, DSPP PCM, SPORT access, and streamed ISO/raw or multitrack CUE XBus data-sector reads through the shared disc-image layer, with deterministic state. It still needs complete CEL/VDLP behavior, programmable DSPP fidelity, exact MADAM/CLIO timing, complete XBus/CD behavior, CD-DA playback, CHD support and representative compatibility-corpus validation.

Dreamcast is `foundation` and launchable with a user-supplied 2 MiB boot ROM and ISO/raw disc image: a dedicated little-endian SH-4 interpreter provides privileged register banks, exception state and floating-point foundations; the AICA side uses a dedicated ARM7TDMI/Thumb execution core. The machine graph connects 16 MiB main RAM, 8 MiB PowerVR VRAM, 2 MiB sound RAM, HOLLY interrupts, framebuffer scanout, Maple controller DMA, AICA PCM, 128 KiB flash persistence, demand-paged GD-ROM sectors and deterministic state. It still needs complete PowerVR2 tile/raster behavior, AICA DSP/ADPCM fidelity, GD-ROM command/media breadth, SH-4 MMU/cache/on-chip peripherals, exact timing and representative compatibility-corpus validation.

PlayStation 2 is `foundation` and launchable from a raw ELF or ISO9660 image using an explicit HLE boot path. The machine combines the 128-bit R5900/Emotion Engine core with EE RAM/scratchpad, INTC/timer state, optional 4 MiB BIOS mapping, ISO `SYSTEM.CNF`/`BOOT2` discovery, GIF A+D/image transfers, GIF DMAC channel-2 normal and source-chain execution with inline/reference tags, CALL/RET address-stack handling, TIE completion and masked D_STAT interrupts, 4 MiB GS VRAM with a PSMCT32 display path, 8 MiB memory-card persistence and deterministic state. It still needs native BIOS/IOP/SIF/SIO2 execution, VU0/VU1, complete GIF/GS raster and swizzle behavior, the remaining DMAC channels and timing/stall behavior, CDVD breadth, SPU2, exact timing, compressed media and representative compatibility-corpus validation.

GameCube is `foundation` and launchable from raw DOL, ISO or GCM data. The machine uses the reusable PowerPC 750/Gekko engine with paired-single arithmetic, comparisons, merges, quantized PSQ loads/stores, GQR/HID2 controls, hardware-table `fres`/`frsqrte` estimates and paired estimate variants, full-width single-precision FPR operands, Gekko 25-bit frC handling for multiply/FMA, fused scalar/paired FMA rounding, FPSCR rounding/NI behavior, disabled single-precision overflow/underflow/inexact status with FI/FR/XX, FPRF/FPCC classification, NaN-aware ordered/unordered compares, invalid arithmetic flags, and corrected opcode-63 A-form decoding; it connects MEM1/ARAM, Flipper PI/DI/SI/VI/DSP register foundations, a GX write-gather path with CP/BP/XF/indexed loads, VCD/VAT vertex framing, display-list traversal, direct/indexed position-color primitive rasterization into the EFB, XF transform/projection/viewport handling, and BP-driven YUYV EFB-to-XFB copies, HLE BS2 Disc-ID/DTK reconstruction plus streamed DVD DMA with Disc-ID gating, packed Request Error state, seek/stop-motor handling, DTK configuration/status commands, demand-paged 48 kHz stereo DTK ADPCM decoding/mixing and block-timed stream position, four controller ports, YUYV XFB scanout, 48 kHz audio DMA, channel-0/channel-1 EXI transfer-complete and interrupt routing, guest-visible Slot A and Slot B memory-card identification/status/read/program/erase access with independent persistence, channel-2 AD16 ID/read/write trace-register transfers, optional user-supplied 2 MiB IPL ROM access with raw boot-code descrambling and embedded font reads, IPL UART/eUART debug-register handling, and channel-0 chip-select-2 IPL SRAM read/write/DMA access with a deterministic RTC and persistent 0x44-byte system SRAM. It still needs Gekko enabled overflow/underflow/inexact exception-result delivery, paired-single rounding-flag aggregation, pathological subnormal/FMA exponent cases, complete GX/Flipper graphics, DSP microcode, additional EXI devices, sub-frame DTK/drive scheduling, exact timing, compressed disc formats and representative compatibility-corpus validation.

Xbox is `foundation` and launchable from an XBE development image. The machine uses the reusable 32-bit x86 engine, maps 64 MiB shared RAM and the NV2A/MCPX apertures, loads XBE sections, decodes retail/debug entry values, exposes PCRTC framebuffer output, and preserves deterministic state. Kernel services or BIOS/MCPX boot, NV2A PGRAPH/PFIFO, USB input, audio, IDE/DVD/FATX, networking, timing and broad compatibility remain.

Wii is `foundation` and launchable from a homebrew DOL. The machine reuses the Broadway-compatible PowerPC/Gekko substrate with MEM1/MEM2, Flipper/Hollywood windows, a PPC-to-Starlet IPC boundary, XFB presentation, audio surface and deterministic state. IOS/Starlet services, retail disc/NAND loading, Wii Remote/Bluetooth, full GX/DSP behavior and exact timing remain.

Xbox 360 is `foundation` and launchable from a PowerPC64 ELF development image. Three Xenon core objects share 512 MiB of sparse unified memory and deterministic state. SMT, VMX128, MMU/cache/interrupt fidelity, XEX/hypervisor/kernel boot, Xenos/eDRAM, Southbridge, audio, storage, controllers, networking and broad compatibility remain.

PlayStation 3 is `foundation` and launchable from a PowerPC64 ELF development image. The machine executes a Cell PPE path over sparse 256 MiB XDR memory, models 256 MiB RSX local memory and seven visible SPU local stores, and preserves deterministic state. SPU execution/MFC/EIB, PPE threading/vector depth, SELF/LV0/LV1/LV2 boot, RSX rendering, devices, timing and broad compatibility remain.

Wii U is `foundation` and launchable from a PowerPC ELF development image. Three Espresso core objects use sparse 32 MiB MEM1 and 2 GiB MEM2 with explicit guest mapping and deterministic state. Cafe OS/RPX/RPL, Starbuck/IOSU, Latte/GX2, DSP/audio, GamePad/controllers, storage, networking, MMU/cache/interrupt fidelity and broad compatibility remain.

Nintendo Switch is `foundation` and launchable from a standalone NRO homebrew image. Four AArch64 application-core state objects share sparse 4 GiB DRAM; the loader validates NRO text/rodata/data segments and BSS, initializes the homebrew entry ABI, and preserves deterministic state. Horizon HLE now covers the libnx startup foundations used by service management, applet/window control, time, filesystem setup, HID shared-memory/Npad input, and VI/Binder display-layer initialization. Retail Horizon service breadth, NSO/NCA/NPDM and NSP/XCI loading, Maxwell/NVN graphics, full audio, storage, networking, production multicore scheduling/timing and broad compatibility remain. Firmware and keys are user-supplied when future retail paths require them.

All 30 active target systems now have an in-OmniCore launchable machine graph. The project target remains broad compatibility, not a claim that all commercial libraries work today.

## Build and verification

Requirements are Node.js 24+ and a current Rust toolchain with `wasm32-unknown-unknown`, rustfmt, and clippy. On Windows, the build helper can use Rust through WSL when native Cargo is unavailable.

```text
npm install
npm run verify
```

`npm run verify` runs brand-isolation checks, rustfmt, clippy with warnings denied, the Rust test suite, an optimized WASM build, direct WASM ABI/runtime smoke tests, TypeScript checking, and the production Vite build.

Useful commands:

```text
npm run core:test
npm run core:fmt
npm run core:clippy
npm run core:build
npm run core:smoke
npm run dev
```

GitHub Pages rebuilds OmniCore from source on every deployment rather than committing a generated WASM artifact.

## Local content

ROMs, disc images, BIOS files, keys, and firmware are user-supplied. Game/system bytes stay local to the browser and are passed to OmniCore; OmniEmu does not upload them. The project does not distribute copyrighted games, proprietary firmware, or console keys.

See `ARCHITECTURE.md` for the unified runtime design and `ROADMAP.md` for the machine implementation sequence.
